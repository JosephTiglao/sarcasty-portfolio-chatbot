{
  "name": "sarcasty-portfolio-chatbot",
  "description": "AI-powered serverless portfolio chatbot built with AWS Lambda, AWS SAM, Google Gemini, and Groq.",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "groq-sdk": "^1.3.0",
    "vitest": "^4.1.9"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^2.0.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  }
}
